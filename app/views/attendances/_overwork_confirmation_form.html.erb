<div class="modal-dialog modal-lg modal-dialog-center">
    <div class="modal-content">
        <div class="overwork_confirmation_form">
            <h2>残業申請の承認</h2>

            <%= @attendances.count %>
            <% @pending_users.each do |user_name, attendances| %>
            <h1><%= user_name %>からの残業申請</h1>
            <%= user_name %>
            <%= attendances %>
            <% end %>

            <% if false %>
            <%= form_tag(@attendances, remote: true) do |f| %>
            <table class="table-bordered table-striped table-condensed">
                <thead>
                    <thead>
                        <tr>
                            <th width="10%">月</th>
                            <th width="20%">指示者確認（印）</th>
                            <th width="10%">変更</th>
                            <th width="30%">勤怠を承認する</th>
                        </tr>
                    </thead>
                </thead>
                <tbody>
                    <tr>
                        <!--月/日-->
                        <td class="attendance_date">
                            <%= l(@attendance.worked_on,format: :short) %></td>
                        <td class="<%= css_class %>"><%= $days_of_the_week[@attendance.worked_on.wday] %></td>
                        <!--指示者確認（印)--　> { nothing: 0, pending: 1, approval: 2, denial: 3 }
                        <td><%= select :attendance, :name, @seniors, :prompt => "選択してください" %></td>
                        <　!--変更のチェックボックス、チェックがないと変更しても反映されない--　>
                        <td><%= check_box_tag :overday_check %></td>
                        < !-- 勤怠を承認するボタン --　>
                        <td><%= link_to "承認", edit_attendance_path(attendance), remote: true, :style=>"color:white;", class: "btn btn-sm btn-primary" %></td> -->
                    </tr>
                </tbody>
            </table>
            <%= f.submit "変更を送信する", class: "btn btn-primary btn-sm" %>
            <% end %>
            <% end %>
            -->
        </div>
    </div>
</div>